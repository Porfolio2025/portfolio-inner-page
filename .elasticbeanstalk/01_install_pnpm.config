commands:
  01_install_pnpm:
    command: sudo npm install -g pnpm
    test: "[ ! -f /usr/bin/pnpm ]"  # Only install if missing

container_commands:
  02_pnpm_install:
    command: pnpm install
  03_pnpm_build:
    command: pnpm run build