version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install -g pnpm
    build:
      commands:
        - pnpm install
        - pnpm run build
  cache:
    paths:
      - node_modules/**/*
      - ~/.pnpm-store/**/*
